# Shared packages

This directory is reserved for stable contracts shared by at least two applications.

Do not move code here merely to shorten imports. A shared package must have a narrow public API, explicit ownership and its own tests. Product-specific features remain in their owning application.
