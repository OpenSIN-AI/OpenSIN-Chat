apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "opensin.labels" . | nindent 4 }}
  name: {{ include "opensin.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}