apiVersion: v1
kind: Service
metadata:
  name: {{ include "openafd.fullname" . }}
  labels:
    {{- include "openafd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openafd.selectorLabels" . | nindent 4 }}
