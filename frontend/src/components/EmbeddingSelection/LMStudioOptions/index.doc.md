# LM Studio Embedding Options

## Purpose

Renders LM Studio embedding settings for endpoint auto-discovery, model selection, and optional auth-token entry.

## Notes

- The auth token field is controlled through `useProviderEndpointAutoDiscovery`.
- Stored secrets should remain masked in the UI and should not be treated as fresh credentials.
