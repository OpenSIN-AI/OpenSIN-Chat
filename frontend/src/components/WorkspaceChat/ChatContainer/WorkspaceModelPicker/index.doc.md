# Workspace model picker

## Purpose

Displays the active model and opens the workspace model-selection panel for administrators.

## Design notes

- The picker uses the same compact neutral control language as the composer and message actions.
