# Archived deployment record: v1.14.0

This historical deployment record was sanitized because it contained private
operator paths, SSH details, internal service names, and production topology.
The release tag and Git history remain the authoritative source for code
changes. Current public deployment guidance lives under `docs/deployment/`.
