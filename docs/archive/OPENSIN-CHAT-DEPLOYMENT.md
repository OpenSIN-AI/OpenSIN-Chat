# Archived deployment notes

This historical deployment document was retired because it contained
production-specific infrastructure details that do not belong in a public
repository.

Current public deployment guidance lives in `docs/deployment/`. Private host
inventory, SSH configuration, internal ports, container names, tunnel details,
and absolute production paths must be maintained outside Git under protected
operator configuration.
