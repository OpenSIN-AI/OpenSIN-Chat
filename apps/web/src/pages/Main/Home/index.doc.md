# Workspace home

## Purpose

Bootstraps the default workspace and renders the initial chat surface, including its prompt and research shortcuts.

## Design notes

- The home canvas is deliberately flush with the main workspace instead of being nested in a large decorative card.
- Cards remain reserved for discrete actions, which makes the empty workspace easier to scan.
