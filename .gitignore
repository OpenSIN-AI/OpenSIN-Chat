# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-devruntime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local
server/.env

# Common ignores
node_modules
.next/

# This is a Yarn project — npm lockfiles must never be committed. They are
# only generated transiently for `npm audit` (see frontend audit scripts).
package-lock.json
.DS_Store

# SIN-Code tool local state (do not commit)
.sin/
.sin-code/
sin_goal_mode.db
.sin_evidence/
*.pack

# Playwright test results
frontend/test-results/
test-results/

# Environment variables — never commit real env files
.env
.env.*
!.env.example
!.env.*.example
server/.env
docker/.env
docker/.env.*
!docker/.env.example
!docker/.env.supabase.example
docker-opensin/.env

# Runtime storage (DB, Uploads, Vektoren) — niemals committen
server/storage/
server/storage-opensin/
collector/hotdir/
collector/outputs/
!**/.gitkeep
server/coverage/
